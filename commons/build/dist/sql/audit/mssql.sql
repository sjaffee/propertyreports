CREATE TABLE APPLICATION_LOG (ID INTEGER NOT NULL IDENTITY PRIMARY KEY, SESSION_ID INTEGER, TENANT_ID INTEGER, HOSTNAME VARCHAR(64), USERNAME VARCHAR(64), LOGIN_TIME DATETIME, EVENT_TIME DATETIME, EVENT_TYPE VARCHAR(64), MODEL_CLASS VARCHAR(64), MODEL_UUID VARCHAR(36), PARENT_UUID VARCHAR(36), VALUE_DESC VARCHAR(255), PREV_VALUE VARCHAR(MAX), NEW_VALUE VARCHAR(MAX));
